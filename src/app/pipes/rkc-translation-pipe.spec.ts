import { RkcTranslationPipe } from './rkc-translation-pipe';

describe('RkcTranslationPipe', () => {
  it('create an instance', () => {
    const pipe = new RkcTranslationPipe();
    expect(pipe).toBeTruthy();
  });
});
